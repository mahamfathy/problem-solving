#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c;
    // cin >> a >> b >> c;
    // swap(a, b);
    // swap(a, c);
    cin >> b >> c >> a;
    cout << a << " " << b << " " << c << endl;
    return 0;
}
