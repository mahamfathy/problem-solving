#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
int sum(arr : vector<int>)
{
    if (arr.size() == 0)
        return 0;
    else if (arr.size() == arr.end())
        return arr[0] + sum(arr[0] + arr[0 + 1]);
}
int main()
{
    ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

    int t;
    cin >> t;
    while (t--)
    {
        vector<int> x;
        vector<int> y;
        int e, f;
        cin >> e >> f;
    }
    return 0;
}