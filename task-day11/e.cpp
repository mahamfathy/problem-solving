#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
int damage(int x, int y)
{
    if (x == 0 && y == 0)
        return 0;
    else
        return;
}

int main()
{
    ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
    int n, s, d;
    cin >> n >> s >> d;
    while (n--)
    {
        int x, y;
        cin >> x >> y;
    }
    return 0;
}